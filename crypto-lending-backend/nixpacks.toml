[phases.setup]
nixPkgs = ["python39", "poetry"]

[phases.install]
cmds = ["poetry install --only=main"]

[phases.build]
cmds = ["poetry run python -c 'print(\"Build complete\")'"]

[start]
cmd = "poetry run uvicorn app.main:app --host 0.0.0.0 --port $PORT"
